// SiteScout config — set WORKER_URL to your deployed Cloudflare Worker.
// After `wrangler deploy`, paste the printed URL here and commit.
window.SITESCOUT_CONFIG = {
  WORKER_URL: "https://sitescout.travishughes-836.workers.dev",
};
